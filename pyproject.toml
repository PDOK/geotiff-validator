[build-system]
requires = ["setuptools>=42.0,!=58.*,!=59.*,!=60.*,!=61.*", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pdok-geotiff-validator"
description = "Validate GeoTIFF files according to PDOK requirements and recommendations"
keywords = ["geotiff-validator", "geotiff", "tiff", "pdok"]
license = {text = "EUPL"}
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dynamic = ["version", "readme"]
dependencies = [
    "setuptools>=42.0,!=58.*,!=59.*,!=60.*,!=61.*",
    "Click >= 8.0",
    "click-log >=0.3",
    "gdal >=3.8",
    "minio",
    "pydantic >= 2.9",
    "pyyaml",
    "semver >= 3.0.2",
]
requires-python = ">=3.10"

[project.optional-dependencies]
test = [
    "black == 22.3.0",
    "flake8",
    "ipdb",
    "ipython",
    "pytest >= 7.0.1",
    "pytest-cov",
    "pytest-flakes",
    "pytest-mock"
]

[project.scripts]
geotiff-validator = "geotiff_validator.cli:cli"

[tool.setuptools.dynamic]
version = {attr = "geotiff_validator.__version__"}
readme = {file = ["README.md"], content-type = "text/markdown"}

[tool.setuptools.packages.find]
include = ["geotiff_validator*"]  # package names should match these glob patterns (["*"] by default)
exclude = ["tests*"]  # exclude packages matching these glob patterns (empty by default)

[tool.pytest.ini_options]
testpaths = [
    "geotiff_validator",
    "tests",
]